#pragma once
#include <iostream>
#include <string>
bool defineLeapYear(int year);
int countDays(int birthday, int monthOfBirth, int day, int month, int year, int yearOfBirth);
int countNumber(int day, int month, int year);